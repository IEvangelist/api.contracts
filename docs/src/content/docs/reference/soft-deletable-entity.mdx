---
title: "SoftDeletableEntity"
description: "Base class for entities that support soft deletion in addition to audit tracking."
sidebar:
  order: 12
  badge:
    text: "class"
    variant: "note"
---

import { readFileSync } from "node:fs";
import { resolve } from "node:path";
import TypeSignature from "../../../components/TypeSignature.astro";
import MemberList from "../../../components/MemberList.astro";
import JsonContract from "../../../components/JsonContract.astro";
import { Aside, Badge } from "@astrojs/starlight/components";

export const schema = JSON.parse(readFileSync(resolve("public/schemas/SampleApi.api-schema.json"), "utf-8"));
export const typeData = schema.types.find(t => t.name === "SoftDeletableEntity");

Base class for entities that support soft deletion in addition to audit tracking.

<Aside type="note">
  Entities derived from  are never physically removed from the data store. Instead, the  flag is set to and  records the time of deletion. Query logic should filter out soft-deleted entities by default.
</Aside>

**Namespace:** `SampleApi` · **Assembly:** `SampleApi`

## Signature

<TypeSignature type={typeData} />

## Members

<MemberList members={typeData.members} />

## JSON Serialization

<JsonContract json={typeData.json} typeName="SoftDeletableEntity" />
