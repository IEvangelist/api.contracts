---
title: "Result<T>"
description: "Encapsulates the outcome of an operation, holding either a success value or an error message."
sidebar:
  order: 11
  badge:
    text: "class"
    variant: "note"
---

import { readFileSync } from "node:fs";
import { resolve } from "node:path";
import TypeSignature from "../../../components/TypeSignature.astro";
import MemberList from "../../../components/MemberList.astro";
import { Aside, Badge } from "@astrojs/starlight/components";

export const schema = JSON.parse(readFileSync(resolve("public/schemas/SampleApi.api-schema.json"), "utf-8"));
export const typeData = schema.types.find(t => t.name === "Result");

Encapsulates the outcome of an operation, holding either a success value or an error message.

<Aside type="note">
  Use the static factory methods  and  to create instances. Callers should inspect  before accessing .
</Aside>

**Namespace:** `SampleApi` · **Assembly:** `SampleApi`

## Signature

<TypeSignature type={typeData} />

## Members

<MemberList members={typeData.members} />
