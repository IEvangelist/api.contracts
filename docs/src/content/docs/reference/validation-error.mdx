---
title: "ValidationError"
description: "Describes a validation error associated with a specific input field."
sidebar:
  order: 15
  badge:
    text: "record"
    variant: "note"
---

import { readFileSync } from "node:fs";
import { resolve } from "node:path";
import TypeSignature from "../../../components/TypeSignature.astro";
import MemberList from "../../../components/MemberList.astro";
import JsonContract from "../../../components/JsonContract.astro";
import { Aside, Badge } from "@astrojs/starlight/components";

export const schema = JSON.parse(readFileSync(resolve("public/schemas/SampleApi.api-schema.json"), "utf-8"));
export const typeData = schema.types.find(t => t.name === "ValidationError");

Describes a validation error associated with a specific input field.

<Aside type="note">
  Validation errors are typically collected during request processing and returned as part of a  response.
</Aside>

**Namespace:** `SampleApi` · **Assembly:** `SampleApi`

## Signature

<TypeSignature type={typeData} />

## Members

<MemberList members={typeData.members} />

## JSON Serialization

<JsonContract json={typeData.json} typeName="ValidationError" />
