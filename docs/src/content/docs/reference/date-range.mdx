---
title: "DateRange"
description: "Represents an inclusive date range with a start and end date."
sidebar:
  order: 17
  badge:
    text: "struct"
    variant: "note"
---

import { readFileSync } from "node:fs";
import { resolve } from "node:path";
import TypeSignature from "../../../components/TypeSignature.astro";
import MemberList from "../../../components/MemberList.astro";
import JsonContract from "../../../components/JsonContract.astro";
import { Aside, Badge } from "@astrojs/starlight/components";

export const schema = JSON.parse(readFileSync(resolve("public/schemas/SampleApi.api-schema.json"), "utf-8"));
export const typeData = schema.types.find(t => t.name === "DateRange");

Represents an inclusive date range with a start and end date.

<Aside type="note">
  is a readonly struct that implements for efficient value-based comparisons. The  property provides the calculated time span between the two boundary dates. A  is considered valid when  is less than or equal to .
</Aside>

**Namespace:** `SampleApi` · **Assembly:** `SampleApi`

## Signature

<TypeSignature type={typeData} />

## Members

<MemberList members={typeData.members} />

## JSON Serialization

<JsonContract json={typeData.json} typeName="DateRange" />
