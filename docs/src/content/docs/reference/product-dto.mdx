---
title: "ProductDto"
description: "A data transfer object for product information, annotated with System.Text.Json serialization attributes."
sidebar:
  order: 9
  badge:
    text: "class"
    variant: "note"
---

import { readFileSync } from "node:fs";
import { resolve } from "node:path";
import TypeSignature from "../../../components/TypeSignature.astro";
import MemberList from "../../../components/MemberList.astro";
import JsonContract from "../../../components/JsonContract.astro";
import { Aside, Badge } from "@astrojs/starlight/components";

export const schema = JSON.parse(readFileSync(resolve("public/schemas/SampleApi.api-schema.json"), "utf-8"));
export const typeData = schema.types.find(t => t.name === "ProductDto");

A data transfer object for product information, annotated with System.Text.Json serialization attributes.

<Aside type="note">
  This DTO demonstrates common JSON customization patterns: to control the serialized property names. to exclude properties from serialization. to enforce presence during deserialization. to apply a custom converter.
</Aside>

**Namespace:** `SampleApi` · **Assembly:** `SampleApi`

## Signature

<TypeSignature type={typeData} />

## Members

<MemberList members={typeData.members} />

## JSON Serialization

<JsonContract json={typeData.json} typeName="ProductDto" />
